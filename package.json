{
  "name": "questerix-help-docs",
  "version": "0.1.0",
  "description": "Questerix user help center — guides for parents, teachers, and admins.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview",
    "screenshots:capture": "node scripts/capture-screenshots.mjs",
    "screenshots:check": "node scripts/check-screenshots.mjs",
    "docs:check": "node scripts/check-doc-rules.mjs"
  },
  "devDependencies": {
    "playwright": "^1.58.2",
    "vitepress": "^1.5.0"
  }
}
