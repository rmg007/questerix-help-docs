{
  "name": "questerix-help-docs",
  "version": "0.1.0",
  "description": "Questerix user help center — guides for parents, teachers, and admins.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview",
    "screenshots:capture": "node scripts/capture-screenshots.mjs",
    "screenshots:check": "node scripts/check-screenshots.mjs",
    "docs:check": "node scripts/check-doc-rules.mjs",
    "lint": "eslint .",
    "format": "prettier --write .",
    "cleanup:screenshots": "node scripts/cleanup-screenshots.mjs"
  },
  "devDependencies": {
    "eslint": "^10.0.2",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-prettier": "^5.5.5",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "playwright": "^1.58.2",
    "prettier": "^3.8.1",
    "vitepress": "^1.5.0"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.js": [
      "npm run build",
      "npm run screenshots:check"
    ]
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "winston": "^3.19.0"
  }
}
